# Repo Identity

Brand: ApprovalPrep

Repo: `approvalprep`

Main domain: `ApprovalPrep.com`

Canonical URL: `https://approvalprep.com`

EMD redirects:

- `LetterOfExplanation.com` redirects to canonical ApprovalPrep letter pages.
- `EmploymentVerificationLetter.com` redirects to canonical ApprovalPrep employment verification pages.

Category: Approval prep

Stack: Astro static-first plus Cloudflare Pages.

Persistence: GitHub-native JSON and Markdown.

V1 has no user accounts and saves no customer documents.

Credit stance: self-service tools only. ApprovalPrep is not a credit repair company.
