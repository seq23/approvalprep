# Download Asset Registry

Runtime paid downloads should be stored in R2 as private objects. Public `/public/downloads` files remain seed/fallback assets only. The download API verifies entitlement before serving R2 files.
