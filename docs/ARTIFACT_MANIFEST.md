# Artifact Manifest

Artifact pattern: `approvalprep_BASELINE_07-07-26_<SHA>.zip`

Required checks:

- ZIP opens.
- ZIP root is `approvalprep/`.
- `package.json` name is `approvalprep`.
- `_artifact_validation_manifest.json` records SHA and validation status.
- Final status is honest.
