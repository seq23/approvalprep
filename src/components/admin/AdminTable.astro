---
const { headers = [], rows = [] } = Astro.props;
---
<table>
  <thead><tr>{headers.map((h) => <th>{h}</th>)}</tr></thead>
  <tbody>{rows.map((row) => <tr>{row.map((cell) => <td set:html={cell}></td>)}</tr>)}</tbody>
</table>
