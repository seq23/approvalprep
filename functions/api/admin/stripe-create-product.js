import { json, readJson } from '../../_runtime/responses.js';
import { requireAdmin, audit } from '../../_runtime/admin.js';
import { createStripeProduct } from '../../_runtime/stripe.js';
export async function onRequestPost({ request, env }){ const auth=await requireAdmin(request,env); if(!auth.ok) return auth.response; if(!env.PRODUCTS_DB) return json({ok:false,error:'D1_BINDING_REQUIRED'},500); const b=await readJson(request); const id=b.id||b.slug; const before=await env.PRODUCTS_DB.prepare('SELECT * FROM products WHERE id=? OR slug=?').bind(id,id).first(); if(!before) return json({ok:false,error:'PRODUCT_NOT_FOUND'},404); try{ const sp=await createStripeProduct(env,before.name,before.long_description||before.short_description); await env.PRODUCTS_DB.prepare('UPDATE products SET stripe_live_product_id=?, updated_at=? WHERE id=?').bind(sp.id,new Date().toISOString(),before.id).run(); const after=await env.PRODUCTS_DB.prepare('SELECT * FROM products WHERE id=?').bind(before.id).first(); await audit(env,'stripe.product.create',before.id,before,after,request); return json({ok:true,stripeProduct:sp,product:after}); }catch(e){ return json({ok:false,error:'STRIPE_CREATE_PRODUCT_FAILED',message:e.message},502); }}
